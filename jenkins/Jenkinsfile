pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'date'
                sh 'echo "Hello from jenkins"'
            }
        }
    }
}

